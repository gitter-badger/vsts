{
  "name": "outsystemsextpackage",
  "private": true,
  "version": "0.0.1",  
  "description": "Outsystems Extension Package",
  "scripts": {
    "package": "npm run clean && npm run compile && tfx extension create --rev-version --manifest-globs",
    "gallery-publish": "tfx extension publish --rev-version",
    "install-task-lib" : "cd outsystemsreleasetask\\outsystemsreleasetaskV4 && npm install --save-dev",
    "compile": "tsc -p outsystemsreleasetask\\outsystemsreleasetaskV4",
    "clean": "rimraf ./*.vsix"
  },
  "devDependencies": {
    "tfx-cli": "^0.4.5",
    "typescript": "^2.3.2"
  }
}